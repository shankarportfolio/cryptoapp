import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['deythere.com', 'www.cryptopolitan.com', 'en.bitcoinsistemi.com', 'www.forbes.com', 'cryptoslate.com', 'www.bitcoinsistemi.com', 'imageio.forbes.com', 'en.coinotag.com'],
  },
};

export default nextConfig;